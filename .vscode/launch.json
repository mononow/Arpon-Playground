{
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch CGPoint",
      "skipFiles": ["<node_internals>/**"],
      "program": "${file}",
      "preLaunchTask": "tsc: build - arpon/tsconfig.json",
      "outFiles": ["${workspaceFolder}/arpon/out/**/*.js"]
    }
  ]
}
