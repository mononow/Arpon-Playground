// import View from './fundation/View';

class Text {
  private stored!: string;

  constructor(verbatim = '') {
    this.stored = verbatim;
  }

  /**
   * @topic Multiline
   *
   */
  /* lineLimit(PartialRangeThrough<Int>) : View

  lineLimit(ClosedRange<Int>) : View

  lineLimit(PartialRangeFrom<Int>) : View

  lineLimit(Int?) : View

  lineLimit(Int, reservesSpace: Bool) : View

  lineSpacing(CGFloat) : View

  multilineTextAlignment(TextAlignment) : View */
}

export default Text;
